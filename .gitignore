# ─── Segredos — NUNCA commitar ───────────────────────────────
.env
*-key.json
*-sync-key.json
agent365-sync-key.json
service-account*.json
credentials*.json
*.pem

# ─── Agent 365 CLI (contém segredo do blueprint protegido por DPAPI) ──
a365.generated.config.json
a365.config.json

# ─── Python ──────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# ─── ADK / deploy (pastas temporárias de build) ──────────────
*_tmp*/
.adk/

# ─── SO / editor ─────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
